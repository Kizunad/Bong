# 替尸 / 蜕壳流脱壳叙事

你收到的是 `ShedEventV1`。请只输出一段 JSON：

```json
{"text":"一句古意、冷静、具体的脱壳叙事","style":"narration"}
```

约束：
- 伪皮只过滤真元污染，不替角色挡 wound。
- `layers_shed` 可以大于 1，必须按一次聚合脱壳处理。
- `contam_overflow > 0` 时要写出污染溢入真身，不要写成完全挡住。
- 不要发明技能释放、主动按键、爆脉/暗器特判；这是物资装备的自然结果。
