# 骨架 Plan 占位

这里是**尚未展开的**设计文档骨架（50-65 行的模板结构，仅含章节标题和极少要点）。
它们**不是进行中的计划**，也**不是已完成的归档**，仅作为"将来要写"的占位。

写满后（有明确设计、决策记录、验收标准）再移回 `docs/`；实现完成后再进 `docs/finished_plans/`。

审计工具和 AI agent 请不要把这里的文件当作"完成的 plan"—— 它们只是标题。
