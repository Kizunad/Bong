# Skeleton 未决事项登记

本目录下 plan 在展开/落地过程中累积的**延后处理**事项。每条带 plan 锚点 + 上下文，后续回来解决时直接对号入座。

---

## plan-tribulation-v1

- [ ] **半步化虚 buff 强度**：当前 +10% 真元上限 / +200 年寿元是占位。Phase 1-3 已上线，可观察"卡在半步化虚"的玩家比例后调整；名额空出时可重渡的升级机制也待确认（已在 plan-tribulation-v1 §8 标注）

---

> **约定**：每解决一条就从这里删。新增延后事项请直接追加到对应 plan 段，保持扁平。
>
> **已转为独立骨架（截至 2026-05-08）**：
> - `plan-alchemy-client-v1`（炼丹系统 Fabric 客户端接入）→ `finished_plans/`
> - `plan-niche-defense-v1`（灵龛主动防御）→ `finished_plans/`
> - `plan-fauna-v1`（妖兽骨系材料）→ `finished_plans/`
> - `plan-spiritwood-v1`（灵木材料体系）→ `finished_plans/`
> - `plan-spirit-eye-v1`（灵眼系统）→ `finished_plans/`
> - `plan-botany-agent-v1`（植物生态快照接入天道 agent）→ `finished_plans/`
> - `plan-lifespan-v1`（寿元精细化）→ `finished_plans/`
> - `plan-anticheat-v1`（AntiCheatCounter）→ `finished_plans/`
> - `plan-alchemy-v2`（丹方残卷 / 品阶铭文 / AutoProfile）→ `finished_plans/`
> - `plan-inventory-v2`（Tarkov grid placement）→ `finished_plans/`
> - `plan-qi-physics-v1`（修仙物理底盘）→ `finished_plans/`
> - `plan-qi-physics-patch-v1`（qi-physics 迁移收口）→ `finished_plans/`
> - `plan-woliu-v2`（涡流功法五招）→ `plans-skeleton/`
> - `plan-dugu-v2`（毒蛊功法五招）→ `plans-skeleton/`
> - `plan-craft-v1`（通用手搓底盘）→ `finished_plans/`
> - `plan-tuike-v2`（替尸蜕壳功法三招）→ `plans-skeleton/`
> - `plan-baomai-v3`（体修爆脉功法五招）→ `plans-skeleton/`
> - `plan-anqi-v2`（器修暗器功法五招）→ `plans-skeleton/`
> - `plan-zhenmai-v2`（截脉震爆功法五招）→ `plans-skeleton/`
> - `plan-meridian-severed-v1`（经脉永久 SEVERED 通用底盘）→ `finished_plans/`
> - `plan-zhenfa-v2`（地师阵法 5 类补全）→ `plans-skeleton/`
> - `plan-yidao-v1`（医道功法支援流派）→ `plans-skeleton/`
> - `plan-npc-fixups-v1`（NPC 三 bug fastlane）→ `plans-skeleton/`（2026-05-08 新建）
> - `plan-npc-fixups-v2`（NPC 第二批 ECS lifecycle bug）→ `plans-skeleton/`（2026-05-08 新建）
> - `plan-npc-perf-v1`（NPC 性能恢复）→ `finished_plans/`
> - `plan-npc-virtualize-v1`（二态 MVP）→ `docs/`（active）
> - `plan-npc-virtualize-v2`（Drowsy 三态扩展）→ `plans-skeleton/`（2026-05-08 新建；v1 P3 实测触发条件满足后启动）
