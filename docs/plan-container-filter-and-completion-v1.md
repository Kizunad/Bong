# plan-container-filter-and-completion-v1 — owner-instance 容器筛选、durable settlement 与九容器闭环

> **主题**：在 `ContainerState.owner_instance_id` + `PlayerInventory.containers` 平展模型上统一 owner/filter、普通 prepared mutation；以 durable source settlement 原子结算矿物/锻造的源、物品、经验和掉落；最终交付九容器与 client 预提示。
> **状态**：Active。P0 已于 2026-06-13 合入；P1A 有少量 resolver/grant 代码超前但未闭环；P1B durable source settlement 未开始；P2/P3/P4 未开始。
> **历史证据**：P0 = PR #526，merge commit `3161ccf0ba1ff25d5ab781e654667090b0e143ac`（2026-06-13），仅证明 P0 数据模型和测试，绝不外推 P1A–P4。

## 阶段总览

| 阶段 | 内容 | 状态 | 验收日期 |
|---|---|---|---|
| P0 | `ItemCategory` 三变体、`ContainerAcceptFilter`、`ContainerSpec.accept_filter`、TOML `accept`、`item_passes_filter` | ✅ | 2026-06-13 |
| P1A | process-local prepared core：owner/category/filter/fit、普通 move/swap、非 durable ingress 与 owner-loss staged transaction | 🔄 | |
| P1B | mineral/forge durable source settlement：stable ID、source/WAL、单事务 inventory/skill/drop/source/station 与恢复 | ⬜ | |
| P2 | freshness transition、integrity/reconciliation、唯一 snapshot producer→reader/outbox/emitter | ⬜ | |
| P3 | 暗器 category 与恰好九个随身 `ContainerSpec` | ⬜ | |
| P4 | filter/`acceptance_lock` wire、client 预提示、e2e、真实 Finish Evidence 与归档 | ⬜ | |

> P1A 的 `🔄` 只表示既有 `container_accepts_runtime_grant` 等局部代码可复用；权威 move/swap、全入口、owner-loss 未收敛。P1B 不存在 durable 实现；现有 `MineralDropEvent` consumer 和 runtime `ForgeSessionId` 不得作为完成证据。

## 接入面（防孤岛）

- **进料**：`server/src/inventory/mod.rs::{PlayerInventory,ContainerState,ContainerAcceptFilter,item_passes_filter}`；`InventoryMoveIntent → handle_inventory_move → apply_inventory_move_with_race`；death/broken/remains/treasure/trade/full-transfer/bone-coin/`ExternalContainer`；mineral `break_handler.rs` 和 forge `session.rs`/`station.rs` source state。
- **durable 保存面**：`durable_block_ownership`/`DurableBlockOwnershipIndex` 是 durable physical ownership 的唯一权威；`mineral_sources`/`MineralOreNode`/`MineralOreIndex`、`forge_stations`/`WeaponForgeStation`、`ForgeSession`/`ForgeSessions` 都只是由 manifest owner 约束的 source/station 或 ECS projection。复用 `player/state.rs::{load_player_inventory_from_sqlite,persist_player_inventory_json_in_transaction}`、`inventories`、`player_skills`、`DroppedLootRegistry`/`dropped_loot`。P1B 必增 transaction-local skill/source/drop/manifest helper，禁止拼接现有独立 save API。
- **出料**：P1A 普通路径只写 process-local live commit；P1B ordinary terminal 只经 `PreparedOrdinaryBreak` 唯一 commit owner 安装 receipt/AIR/drop/furniture，durable terminal 只 CAS immutable WAL 至 `CommittedPendingInstall`，再完成 canonical `DurableInstallReceipt`/ack；`ProjectionOutbox` 只延迟投影且不得阻塞 ack。P2 才统一 snapshot；P4 才 wire/client。
- **共享类型/event**：复用 `ItemCategory`、`InventoryLocationV1`、`InventoryMoveOutcome`、`ContainerFreshnessBehavior`、`SkillSet`、`DroppedLootRegistry`。P1A 是 borrowed `PreparedInventoryMutation<'a>`；P1B 固定为 owned serializable `PreparedInventorySettlement`、公共 `InventorySettlementId` 与 `InventorySettlementCommittedEvent`。Entity/tick/runtime `ForgeSessionId` 绝不可作 durable key。
- **跨仓库契约**：P1A/P1B 不新增、迁移、锁定或验收 inventory snapshot sender、EventWriter/outbox、final snapshot once；既有 request/resync 只由 P2 替换。P4 才动 `ContainerSnapshotV1`/`InventoryItemViewV1`、protobuf、TypeBox 和 client。
- **worldview / qi**：`coin_box` 只做骨币分类；本 plan 不新增 qi 公式，锻造既有真元流仍走 ledger；freshness 复用既有 shelflife 行为。

## 当前架构基线与范围边界

1. `owner_instance_id` 为唯一 owner 真源；`pack_<instance_id>` 只为 UI/protocol ID，绝不从字符串反推、补写或授权 owner。
2. `classify_container_ownership`/`find_live_container_owner` 是唯一 resolver。静态 allowlist 仅 `body_pocket`、`main_pack`、`small_pouch`、`front_satchel`；live owner 仅 worn、held、hotbar、`body_pocket`。其它 grid、任意 `pack_*` 与 owner 自身容器不可作为 live surface。
3. 每个 `InventoryLocationV1::Container` from/to 先过同一结构门；from 不做 filter，to 才做 filter；owner 进入自身 `pack_<owner_id>`（move/swap 两方向）为 typed `ContainerOwnerSelfContainment`，零副作用。
4. `resolve_item_category(item,item_registry,mineral_registry)` 是唯一 category authority：registered template 取 canonical category；未注册仅 canonical `mineral_id`、registry hit、精确 `mineral_<canonical>` template 三者齐备才 Mineral；spoof/unknown/mismatch typed invalid，绝不 fallback `Misc`。
5. `water_skin` 已移出 scope，仅保留“历史移交 satiety/hydration”的说明；不得出现于容器数量、grid/filter、`ContainerSpec`、阶段交付、测试或 PR scope。`trade_crate`/`dead_drop_box` 是 placeable 边界，`herb_crate_placed` 为独立 placed twin，P3 不实现转换。
6. nested/session 容器路线已否决，唯一玩家移动入口仍是 `InventoryMoveIntent`；forge runtime session 只投影 durable `ForgeAttemptId`。
7. P1A 固定内部 `ContainerAcceptanceLockReason::{OwnerMissing, OwnerNotFound, OwnerInvalid}`，判定口径即上第 2 条 resolver 的 owner allowlist/eligible surface 三态；P2 持久化 `integrity_lock.reason` 与 P4 `acceptance_lock.reason` 均取该类型，wire 编码见 P4。

## P0 — 筛选数据模型 ✅ 2026-06-13

完成 `ItemCategory::{Mineral,Anqi,Liquid}`、`ContainerAcceptFilter::{Category,TemplatePrefix}`、`ContainerSpec.accept_filter`、TOML accept 和 `item_passes_filter` 的 None/empty/OR 及正反测试。runtime grant、move、freshness、durable source、九容器、wire/client 均不属于 P0。

## P1A — process-local owner/filter prepared core 🔄

### 可核验交付物

1. 在 `server/src/inventory/mod.rs` 固定 `classify_container_ownership`、`find_live_container_owner`、`resolve_container_acceptance`、`resolve_item_category`，落实上述 allowlist、eligible surface、typed corruption、目标 filter OR；`rebuild_containers_from_equipment` 复用它。
2. 版本化 owner migration 替换 load-time prefix backfill：从 `inventories.schema_version` v2 起，严格 parser 只接受 canonical `pack_<u64>`，拒 `pack_01`/空白/符号/溢出；仅唯一 eligible live owner 且有效 `ContainerSpec` 可写 `owner_instance_id`。inventory JSON SQLite commit 成功后才升版；失败保留 pending；不 bump revision、不发 snapshot。
3. `PreparedInventoryMutation<'a>` 为 private、non-Clone、`#[must_use]` 的借用 process-local **单 `PlayerInventory`** 载体：prepare→`commit_prepared_inventory_mutation(self, drops: &mut DroppedLootRegistry, outcomes: &mut EventWriter<InventoryMoveOutcome>) -> CommittedInventoryEffects` 独占 `&'a mut PlayerInventory`。prepare 只持 staged inventory、route、merge/push、`Vec<PreparedInventoryDrop>`、一次 outcome 与 debug `original_revision`；commit 的调用 handler 是唯一 effect owner，显式传入仍由其持有的 drop/outcome sinks，替换 inventory 后不可失败地安装 effects。载体不得存 resource/event、跨 handler/tick/DB 或并存，revision 恰 +1；无 CAS。
4. `PreparedInventoryMutation` **只**服务普通 process-local 单 inventory move/swap/非 durable ingress。它不是 crash atomic，也绝不神奇覆盖 multi-resource：`ExternalContainerPreparedMove`（player+`ExternalContainer`）、`PreparedPlayerTransfer`（source+target `PlayerInventory`+`DroppedLootRegistry`+`DropContext`）、`PreparedOwnerLossSpill`（owner inventory+drop registry+outcome）及 `PreparedBoneCoinTransaction`（inventory+bone-coin storage+ledger+drop+event）是各自显式命名、只在调用方同时独占全部参与资源期间构造并 commit 的 carrier。每种 carrier 列出 staged candidates、唯一 commit owner、每个 player revision +1 的规则；prepare 任何失败或 commit 前退出时所有参与资源深比较不变。P1B `PreparedInventorySettlement` 另行持久化，禁止调用上述任一 process-local commit。
5. 新增 `PreparedInventorySettlement`：owned、可序列化、无 live borrow、不得直接 commit/outcome；以同一 resolver/filter/fit/drop policy 产生 P1B WAL 所需 candidate `PlayerInventory`、candidate `SkillSet`、candidate drops/result/source/station snapshots，交给 P1B 唯一 durable commit API。不得让 borrowed mutation 跨 tick/SQLite。
6. P1A 的 `InventoryItemPrepareFn` 仅原样携带 freshness 字段；不得执行 Freeze/Normal 或任何 transition。ordinary move/swap、owner-loss spill/drop、durable settlement 同样遵守；P2 才替换/扩展 hook。
7. 普通 move/swap、四 automatic ingress 与非 durable `_or_ground` ingress 仅可使用 `PreparedInventoryMutation`。`ExternalContainer` 双资源 move 固定用 `ExternalContainerPreparedMove`；owner-loss（discard/morph/backpack break）固定用 `PreparedOwnerLossSpill` 做 detach→rebuild→spill→drop→outcome；death/TSY death、broken recovery、remains、treasure deactivate、trade、full transfer、bone-coin 按第 4 项各自的 explicit multi-resource carrier。每条路径的 prepare 失败均不得改变任一 live resource，成功 revision 按各自 carrier 的规则精确推进。
8. `transfer_all_inventory_contents` 固定用 `PreparedPlayerTransfer`，live owner 交易必 `ContainerOwnerTradeForbidden`；bone coin craft 固定用 `PreparedBoneCoinTransaction`，且其 ledger transfer 继续走 `qi_physics::ledger`。NPC/world 自产不是玩家 ingress。
9. 不新增、不迁移、不锁定、不验收 snapshot sender；不要求 EventWriter/outbox/final snapshot once；保持既有 request/resync 给 P2。

### 饱和测试

普通 `PreparedInventoryMutation` 的 new/partial/full merge、move/swap 双向 filter、self-containment、owner-loss/rebuild/spill/drop、每种非 durable ingress；`ExternalContainerPreparedMove`、`PreparedPlayerTransfer`、`PreparedOwnerLossSpill`、`PreparedBoneCoinTransaction` 分别做 compile/API pin，证明只能在持有其全部命名资源时 commit、effect sink 只能消费一次。每个 carrier 覆盖成功 revision 精确 +1 和每一 resolver/category/bounds/collision/weight/drop-context/ledger 前置失败的所有参与资源深比较不变；ordinary drop/outcome 恰一次。另 pin private non-Clone `#[must_use]`、活借用期间不能二借/改 revision/layout；freshness raw 字段完全不变；不测 transition、snapshot sender/outbox/final snapshot。

## P1B — mineral/forge durable source settlement ⬜

### Stable ID、durable mineral source/WAL 与 station 状态

1. 公共 `InventorySettlementId` 为 UUID/ULID；retry/reconnect/reload/notification 全复用。禁止 Entity、tick、runtime `ForgeSessionId`。
2. 新增 SQLite `mineral_sources` 为矿物唯一 durable **source** 真源；物理 durable ownership 仍唯一取 `durable_block_ownership`。复合 identity 固定为 dimension、`BlockPos`、canonical `mineral_id`、`generation_id`；row 至少持 `extraction_seq`、remaining、`state: Active|Exhausted|Respawning`、revision、nullable respawn deadline、schema_version 与 audit timestamps。初始 anchor/旧档迁移、每次 extraction terminal、exhaustion、respawn generation +1 必须在同一 durable `BEGIN IMMEDIATE` create/CAS 推进 `mineral_sources` 与对应 `DurableBlockOwnership` manifest（terminal remove/exhaust 时同事务更新/删除 manifest），commit 后才幂等投影 `MineralOreNode`/`MineralOreIndex`/block。settlement source unique key 必须引用该 row 的 generation + extraction_seq + revision；同坐标再生绝不串账。
3. `data/minerals/exhausted.json` 和 `ExhaustedMineralsLog` 只做一次性导入：导入时按 anchor 维度赋值、分配 generation 并落 `mineral_sources` 后立即退役；之后它们最多是从 SQLite 派生的 projection/删除路径，绝不能独立决定 spawn、exhaustion 或 respawn。坏/歧义 legacy entry fail-closed，不以空 log 猜测新 source。
4. `ForgeStationId { dimension: DimensionKind, pos: BlockPos, generation: u64 }` 是唯一锻炉 durable identity，**owner 不入 ID**。SQLite `forge_stations` 是真源，ECS `WeaponForgeStation` 与 ANVIL block 只为投影。row 至少持完整 ID、tier、nullable canonical owner player identity、integrity、revision、`state: Active|Removed`、nullable `active_attempt_id`、schema_version 与 audit timestamps。
5. `ForgeStationPlacementId` 必须稳定且可重试去重（由被消费的唯一 station item instance + target `ForgeStationId`/immutable request key 派生）。placement 在同一 `BEGIN IMMEDIATE` 以内 canonical player + expected inventory revision CAS 精确 station item instance（删除或递减）、持久 candidate inventory/revision、按 `(dimension,pos)` 分配 generation、写 `forge_stations Active`、对应 `DurableBlockOwnership { owner: ForgeStation { station_id } }` manifest 及 placement audit/idempotency row；commit 后才安装**同一** inventory snapshot 并投影 ECS/ANVIL。SQL/CAS 失败零扣物/零 station/零 manifest；commit/install 间 crash 从 DB 重装；重复 request 返回既有 placement outcome，绝不二扣。
6. 新增持久 `ForgeAttemptId`：inputs accept/lock 的同一 `BEGIN IMMEDIATE` 必须以 expected inventory revision CAS 精确 input instance IDs/stack slices 与数量，持久化扣除后的 candidate inventory、immutable escrow payload、完整 `ForgeStationId`、canonical player、expected station revision、Prepared attempt row，并 CAS station `(id, expected revision, active_attempt_id=None)` 为 `active_attempt_id=attempt` 和 revision +1。runtime `ForgeSessionId`/Entity 只作投影；terminal 只能消费 attempt escrow，禁止重读 live inventory。
7. durable source/settlement WAL（版本化 `inventory_settlements`）保存 settlement ID、canonical player identity、**immutable before snapshots**、after candidates、source/attempt/station/manifest candidates、stable ground-drop payload、完整 item/no-item payload、XP delta、drop context、forge quality/color/effects/tier/consecration、全部 expected target revision/hash、`wal_schema_version`、schema migrator version、payload SHA-256、install marker、结果/outcome 与 audit。状态唯一固定为 `Prepared`、`CommittedPendingInstall`、`CommittedStored`、`CommittedDroppedToGround`、`CommittedNoItem`、`QuarantinedCorrupt`；Perfect/Good/Flawed 只能 Stored/Dropped，Waste/Explode 只能 NoItem，Explode 同时持久 station integrity/wear。`Prepared` 的同一 `BEGIN IMMEDIATE` 写 immutable snapshots/candidates/reservations；之后 terminal/recovery 只能反序列化 WAL，不得重算经济或从 live ECS 补 candidate。
8. `InventorySettlementReservation` 以 settlement ID 对 canonical player inventory/skill、mineral source 或 forge attempt、forge station 四类 key 建 durable unique reservation；所有相关 writer 先过 `settlement_writer_gate`，被占资源只允许该 settlement 的 terminal/recovery/repair worker 修改，普通 move/trade/death/autosave/disconnect、mineral extraction、forge start/step/break 一律 typed `SettlementReserved`，不得推进 revision。

### A. Ordinary terminal 单一提交权威（process-local tick atomic）

1. dispatcher 对 ordinary Start/Terminal 在授权前捕获 `BreakAttemptSnapshot.target_identity: TargetIdentity`，而非只缓存 AIR/`BlockState`：`PhysicalBlock { dimension, pos, state: BlockState }` 用于真正由 `ChunkLayer` 承载的地形、spiritwood log 与 forge ANVIL/manifest；`EntityBacked { placement_id: EntityBackedPlacementId, generation: u64, dimension, pos, kind: EntityBackedTargetKind }` 用于 marker 占位而物理层为 AIR 的 `ContainerBlock::{StorageCrate,DeadDrop}` 和 `WorkbenchBlock`。`ExternalContainer` session/`FurnitureRegistry` visual 只可绑定并复核该 entity-backed identity，绝不可代替 target identity；social `SpiritNicheRegistry` 的 Start/Stop reveal 及 spiritwood 均为 physical-world consumer，不可错误改成 marker identity。`Entity::to_bits()`、`placed_at_tick`、session ID 和 transient `Entity` 均不得作跨 tick/reload identity。
2. P1B 新增 SQLite authoritative `entity_backed_placeable_placements`：由 immutable UUIDv7 `EntityBackedPlacementId`、`generation`、dimension、pos、kind、state（Active|Removed）、schema/audit 字段构成；以 `(dimension,pos,kind)` 的单调 generation CAS 记录同址同 kind 每次 replacement。创建 placement 时，在写 marker/`ExternalContainerRegistry`/`FurnitureRegistry` 前原子分配并持久 Active row；hydrate 只从 Active row 重建 marker，并把同一 placement ID/generation 插入 `ContainerBlock`/`WorkbenchBlock`；拆除/despawn/replacement 在任何 visual/session 改动前 CAS Active→Removed，下一次同址放置 generation +1 且新 UUID。当前 `ContainerBlockPlacement`、`ContainerBlock`（`server/src/world/container_block.rs:43-68`）与 `WorkbenchBlock`（`server/src/craft/workbench.rs:38-43`）必须携此值；无 row、重复 Active、kind/dimension/pos 不一致均 fail-closed，不从 AIR、Entity bits 或 tick 推断。
3. ordinary Terminal 只构造 private、`#[must_use]`、non-Clone 的 `PreparedOrdinaryBreak`；它持 immutable `BreakAttemptSnapshot`（含上述 typed identity）、ordinary drop candidate 与 `OrdinaryBreakReceipt` key。prepare 必须同时对 container policy 产出 typed branch：`PreparedOrdinaryBreakDisposition::{OrdinaryReturnAndDrain, DeadDropWardTriggered}`；AIR CAS 与普通 drop **不得**分别消费同一 terminal authorization。
4. `DeadDropWardTriggered` 只可由 `TargetIdentity::EntityBacked { kind: DeadDrop, .. }` 且 breaker != effective owner 产生：组件存在时 effective owner/active 取 `DeadDropWard { owner, ward_active }`；组件**缺失**时必须按生产 `illegal_dead_drop_break_ward` 合成 `DeadDropWard { owner: ContainerBlock.placed_by, ward_active: true }`，因此 missing owner/non-owner 分别等价 active owner/non-owner，绝不降级 ordinary。当前 `DeadDropWard` 只有 `owner` 与 `ward_active`，**不存在 expiry 字段或 expired 分支**；inactive 仅指真实 `ward_active=false`。未来若需 expiry，必须另立 schema + SQLite migration plan，本 plan 不得凭空添加。非 owner active（含 missing ward）冻结 `server/src/world/container_block.rs:235-253` 的现状：先向已打开者发送 `LootContainerCloseReasonV1::ContainerDestroyed`，移除 `ExternalContainerRegistry` session，再将 visual container entity 标记 `Despawned`；**不**写 AIR、**不**返还 dead_drop_box、**不** drain contents/写 ground drop，物理 block 保留。它唯一拥有一次 `CombatEvent` + `ApplyStatusEffectIntent` 半径反噬、`bong:dead_drop_ward_break` VFX 与 `dead_drop_ward_break` audio，且须以 receipt 防止 terminal replay、entity 已 despawn 或 session 已移除时重复的 combat/AV/close side effect；不得向 breaker 或旁观者泄露 contents。
5. 唯一 `commit_prepared_ordinary_break(self, &mut ChunkLayer, &mut PlayerInventory | &mut DroppedLootRegistry, &mut InventoryInstanceIdAllocator, &mut FurnitureRegistry, &mut ExternalContainerRegistry, &mut OrdinaryBreakReceiptStore, &mut EventWriter<OrdinaryBreakApplied>, ...)` 同时持有所有实际参与可变资源；其**第一步**在 block/entity/session/inventory/drop/allocator/ward AV 前复核 receipt 和 authoritative `TargetIdentity`：PhysicalBlock 比较 live dimension/position/full state；EntityBacked 比较 Active persisted placement ID/generation/dimension/pos/kind 与 live marker component，且 session 仍绑定该值。receipt 已存、target 消失/替换/重生、layer/dimension/state/identity 不符或旧 session 指向新 marker，立即 typed `DuplicateOrdinaryBreak`/`StaleAuthorizedBreak`，零 AIR、零 inventory/drop、零 allocator、零 furniture/container/session/ward effect。
6. prepare 完成后 installation 不可失败：`OrdinaryReturnAndDrain` 在同一 exclusive system 依次写 receipt、PhysicalBlock 的 AIR **或** EntityBacked placement Active→Removed、inventory 或 ground-drop、已预留 allocator 结果、container/workbench item return 与 contents drain、furniture/container/session side effect，最后 emit `OrdinaryBreakApplied`；`DeadDropWardTriggered` 只安装其 receipt、session cleanup、entity `Despawned` 和一次 combat/VFX/SFX/audio，绝不执行 ordinary return/drain/AIR。两分支都是 process-local、单 tick atomic；不声称 SQLite/crash atomic，绝不拆成两个 consumers。后续观察者只读 `OrdinaryBreakApplied`，不得给予掉落。
7. 清除 `apply_default_block_break` 与 `apply_block_drops` 并列提交、以 `.before(...)` 维持正确性的旧设计；二者迁为 prepare/纯 drop mapping 或删除。container/workbench 的返还、container items drain、`FurnitureRegistry` 移除必须由普通分支的唯一 commit owner 作为同一候选安装面处理，ward branch 不得无条件共用 drain；prepare 任一失败不留下 AIR、entity/session 或 AV 后补救。

### B. Durable finality 与延迟 projection 分离

1. terminal worker 只校验 immutable Prepared WAL 后 CAS `Prepared→CommittedPendingInstall`，不重算经济、不读 live source/session、不删 reservation。durable finality 不等待 chunk/ECS/ground entity/玩家未来重连。
2. `DurableInstallReceipt` 只覆盖一个 SQLite `BEGIN IMMEDIATE` 能原子安装的 canonical state：`inventories`/`player_skills` terminal snapshot、mineral source 或 forge attempt/station、`durable_block_ownership` manifest、WAL terminal data 与 stable `ground_drop_outbox` rows。`ProjectionOutbox`/`ProjectionReceipt` 是单独层，只含 chunk block、ECS/index、ground-drop entity、未来 hydration 的 projection id/hash；不得作为 economic acknowledgement 前置。
3. **在线 player 唯一顺序**：reservation 后，唯一 exclusive install worker 经 `SkillMutationGateway` 持有 live `PlayerInventory`/`SkillSet`；仅 before revision/hash 对拍才安装 WAL after candidate，并以 settlement ID + target revision/hash 写 process `LiveInstallReceipt`。Prepared、terminal→live install、live install→canonical install、canonical install→ack 的每个窗口，所有普通 writer 先过 gate 并 typed `SettlementReserved`。接着同 worker 在 `BEGIN IMMEDIATE` 对拍 WAL/LiveInstallReceipt/canonical before 后安装 canonical targets + DurableInstallReceipt/outbox，随后同 transaction ack 并 delete reservations exactly once。live 已装而 canonical 未装时 crash，recovery 仅按 hash/receipt 重放，不许 autosave 或其它 writer 抢跑。
4. **离线 player 唯一顺序**：无 live target，terminal/recovery 直接在同一 SQLite transaction 安装 canonical after snapshot、DurableInstallReceipt/outbox，ack 后释放 reservation；未来 hydration 只读 DB terminal snapshot，绝不因离线等待 reservation。
5. ack 后立即释放经济 reservation：chunk 永不加载、player 永不重连、ground entity 未 spawn 均不能滞留。`ProjectionOutboxKey` 是唯一 canonical identity，精确定义为 `(settlement_id TEXT UUIDv7, payload_ordinal INTEGER)`：`InventorySettlementId` 沿用生产 durable UUIDv7 字符串表示（`Uuid::now_v7().to_string()`），`payload_ordinal` 为该 immutable settlement payload 内从 0 起的非负序号；SQLite `ground_drop_outbox` 必须 `UNIQUE(settlement_id, payload_ordinal)`。projection/replay 首先按此复合键读 row：同 key 且 payload SHA-256 相同才 idempotent，same key/hash 不同一律 `ProjectionQuarantine`；禁止用 `hash(settlement_id,payload_ordinal)` 当 entity、instance 或幂等 identity。
6. `InventoryInstanceIdAllocator` 的 durable version 是**全部** `ItemInstance`、`DroppedLootEntry` 与 `GroundProjectionSurrogateId` producer 的唯一全局线性化 authority；不得为 settlement/outbox surrogate 建第二 allocator、第二 high-water 表或直接自增。现有 `inventory/mod.rs:598-638` 内存 `next` 与 `player/state.rs:3813`/`persistence/mod.rs:5682-` 的 JSON scan high-water 只为迁移基线，P1B 后不得作为 allocation authority。SQLite 建唯一 global sequence row（JS-safe `next_id`）及 `inventory_instance_id_leases(lease_id, owner, process_epoch, start_id, end_id, next_id, consumed_at/created_at)`：每次 runtime allocator 取 lease 在 `BEGIN IMMEDIATE` 内 CAS 推进该唯一 sequence 并持久记录不重叠闭区间 `[start_id,end_id]`；内存 `next_id` 只能消费自己 durable lease。崩溃后的未消费 ID 可永久跳过、绝不回收/转配给不同 identity；达到 `JS_SAFE_INTEGER_MAX` 前后均 fail-closed exhausted，禁止溢出或概率重试。
7. settlement canonical transaction 若需 outbox row，必须在**同一 `BEGIN IMMEDIATE`**先以该 global sequence 为 `(settlement_id,payload_ordinal)` reserve/consume 一个 surrogate，再写 composite-key→surrogate mapping、outbox/WAL/receipt；live allocator lease 也只从同一 sequence 预留。启动/restart 不得从 inventories/dropped JSON 推导或回拨 sequence。因而 settlement→ordinary、ordinary→settlement、并发 lease、crash/restart 均不可重号；同一 composite key 仅在完整 persisted identity + payload SHA-256 相同下幂等返回既有 surrogate，否则 typed quarantine/fail-closed，绝不以 hash 充当 ID。
8. `DroppedLootRegistry` 必须封装 collision-checking `insert_checked`：ID 缺失才插入；同 ID 且完整 `DroppedLootEntry` identity/payload hash 一致才 idempotent；同 ID 异主、异 composite key 或异 payload hash 返回 typed collision 并零覆盖。SQLite `dropped_loot`、outbox surrogate mapping 与 hydration 同样采用 insert-or-verify（禁止现有 `persistence/mod.rs:5594-5631` 的 `ON CONFLICT(instance_id) DO UPDATE` 覆盖）；load 的 `HashMap::insert` 先做同一 identity/hash 校验。P1B 静态 gate 禁止生产 `DroppedLootRegistry.entries.insert`、`HashMap::insert` 对 durable loot、`INSERT OR REPLACE` 与该 UPSERT 形态；仅 registry checked API、受控 test fixture 和 migration adapter 可出现。
9. **生产迁移清单（不可省略）**：把 `inventory/mod.rs::{spawn_template_dropped_loot,inventory discard/owner-loss/external-container}`、`mineral/inventory_grant.rs`、`forge/inventory_bridge.rs`、`alchemy/danxin.rs`、`botany/{mod,harvest}.rs`、`fauna/{drop,daozhan,dying_elder,bone_coin}.rs`、`npc/{loot,tsy_hostile,dormant/relic_hydrate}.rs`、`world/{block_drop,container_block,loot_pool,tsy_container_search,spawn_tutorial}.rs`、`craft/workbench.rs`、`coffin/mod.rs`、`lingtian/systems.rs`、`spiritwood/mod.rs`、`supply_coffin/interact.rs`、`zhenfa/mod.rs`、`combat/{lifecycle,tuike}.rs`、`network/{client_request_handler,craft_emit,tuike_ash_emit}.rs`、`cmd/dev/{give,block_picker}.rs` 的所有 runtime `next_id`/ItemInstance producer 改走同一 lease API；把 `inventory/mod.rs`、`inventory/tsy_loot_spawn.rs`、`fauna/{drop,daozhan,dying_elder}.rs`、`npc/{tsy_hostile,dormant/relic_hydrate}.rs`、`world/container_block.rs` 和 durable hydration 的所有 `DroppedLootRegistry.entries.insert` 改为 `insert_checked`。`inventory/mod.rs:867` 的 resource 初始化改为 durable lease bootstrap，`persistence/mod.rs` migration/transaction helper 成为唯一 sequence/lease/mapping writer；用 static pin 覆盖上述路径和全 `server/src`，不允许漏网 direct producer/insert。
10. startup 先 hydrate `DurableBlockOwnershipIndex`、canonical receipt/outbox，再 recovery；recovery 对 Prepared 重做 terminal，对 pending 只完成 live/canonical receipt 或 ack，对已 ack 只补 projection。`InventorySettlementRecovery` 早于 player hydration/block writers；ProjectionOutbox 可以无限期等待 chunk/player 可用而不阻塞 finality。

### C. `QuarantinedCorrupt` 审计终结路径

1. WAL/receipt 持 before、after、所有 target schema/hash/revision、install marker 与 evidence hash；不一致进入 `QuarantinedCorrupt` 并保 reservation，禁止 after-only 和模糊幂等覆盖。
2. 唯一入口是 operator-only server-side `SettlementRepairAuthority`，不暴露普通 C2S。durable `SettlementRepairRecord` 记录 actor/reason/time、expected quarantine revision CAS、evidence hash、schema migrator version、完整 target hashes 与 action；状态唯一为 `QuarantinedCorrupt→RepairPrepared→CommittedPendingInstall|CompensatedTerminal`。
3. 自动规则固定：canonical targets 全等 after 且 install marker 存在，rebuild receipt 后继续 commit；全等 before 且无 marker，仅经已验证 schema migrator retry immutable candidate；mixed/unknown 必须由 operator 从备份提交一套完整 coherent snapshot，验证全部 target hashes 后选择 `CommitAfter` 或 `CompensateBefore`，禁止局部拍脑袋修。
4. `RepairPrepared` 仍持 reservation，repair/restart 以 repair record + expected revision CAS 幂等；只有最终 ack 或 `CompensatedTerminal` transaction 可 exactly-once 释放。compensation 先写完整 before canonical snapshot/outbox disposition；恢复后资源重新经 gate 可写。

### D. `SkillSet` writer gate（可核验）

1. `SkillMutationGateway` 是唯一 production 写入口，负责 reservation check、candidate revision/hash 对拍和 live install；`Changed<SkillSet>` persistence/emit 只是 observer，不是 gate。除 gateway 实现、实体首次构造/attach（`inventory::attach_inventory_to_joined_clients`、`cultivation::attach_cultivation_to_joined_clients`）及构造测试外，static/compile pin 禁止 production 出现 `&mut SkillSet`、`Mut<SkillSet>`、`Query<... &mut SkillSet ...>`、`get_mut::<SkillSet>` 或字段 mutation。
2. 实地迁移清单固定：`skill::consume_skill_xp_gain`（`server/src/skill/mod.rs:47-76`）、scroll consume 的 `network::client_request_handler::handle_learn_skill_scroll`（`:3055-3079`）、角色终结 `combat::lifecycle::reset_for_new_character`（`:1703-1828`）、dev-only `/technique reset_all` 的 `cmd/dev/reset::reset_progression_state`（`:247-294`）。四者 reservation 时均 typed `SettlementReserved`；`player/state.rs` serialize/upsert、`player/mod.rs` Changed observer、只读 forge/lingtian/botany 不是 writer。
3. 每个 writer 在真实 `main::build_server_app` 对抗测试中分别插入 Prepared、terminal→live install、live install→canonical install、canonical install→ack，断言 ECS `SkillSet` 与 SQLite `player_skills` 都不变且 typed `SettlementReserved`；ack/compensation 后同一 writer恢复可写。

### E. 单一 raw reader、忠实 phase matrix 与注册顺序

1. 仅 `world::block_break::authorize_block_break_intents` 在 production `server/src` 声明 `EventReader<DiggingEvent>`。它以 raw `Start|Stop|Abort` 建不可变 `BreakAttemptSnapshot`（attempt ID、player/identity、dimension、position、完整 original `BlockState`、direction、game mode/tool/permission-distance context、received tick）；重复 Start 结束旧 attempt 为 `Superseded`，无 Start terminal 建 orphan epoch，Creative raw Start 同时派生 synthetic Terminal。envelope 必带 `raw_packet_phase`、`synthetic_completion` 与 per-raw-event dispatch receipt，故一个 raw event 对任一消费者至多一次。
2. ordinary 只消费 authorized Terminal 并走 `PreparedOrdinaryBreak`；mineral 保留现有实码语义：Survival raw Start 建 gathering、Abort 取消、Stop terminal 才 durable settlement，Creative raw Start 派生 terminal cleanup 且零物品。spiritwood 只消费 Survival raw Start 建 session，Creative Start 明确 reject；container block 只在 authorized Terminal 拆/掉物；forge 仅新增 envelope consumer，Start 只 station/permission/busy preflight、Abort 只取消非 terminal 交互、Terminal 只 reserved/active reject 或 Removed durable settlement，不新增 C2S。
3. **workbench 忠实修正**：`craft/workbench.rs` 的打开只走独立 `WorkbenchOpenRequest → handle_workbench_interact` 与实体/三格 Chebyshev 校验；digging 只消费 authorized Terminal，沿现有语义返还/ground-drop `workbench_item` 并拆 visual entity。删除“dig Start 打开、Abort 取消”的臆造。
4. **social 忠实修正**：当前 `detect_spirit_niche_break_attempts` 对同一 raw `Start` 与 raw `Stop` 各发一次 reveal/intrusion，Abort 不发；迁移后必须保持此生产语义。Creative raw Start 派生的 synthetic Terminal 不得再次投递 social；以 `raw_packet_phase` + `synthetic_completion` + dispatch receipt 保证 Creative Start 一次、任意保留的 raw Start→Stop 两次、重复/被 supersede Start 按各 raw event 一次、orphan terminal 按其独立 raw Stop 一次，绝不因 synthetic completion 双发。
5. `world::register` 固定 Recovery → Authorize → Ordinary → DurablePrepare → DurableTerminal → Projection；`configure_sets(Update, (...).chain())` 和 `main::build_server_app` 接线为静态/运行 pin。manifest miss 永远 Ordinary，即使 state 为 IRON_ORE/ANVIL；manifest hit 的 source/index/ECS/block projection 不一致为 `DurableBlockIntegrityFault`，零经济且不降级 ordinary。

### 饱和测试、crash matrix 与生产切流

1. `PreparedOrdinaryBreak` 覆盖 ordinary 成功 exactly once、重复 terminal、PhysicalBlock snapshot state/dimension/layer 变化，以及 EntityBacked container/workbench 的 marker replacement、same pos/same AIR、entity despawn→respawn、placement generation 变化、旧 terminal/duplicate、旧 `ExternalContainer` session 指向新 entity、跨 session；所有 typed `StaleAuthorizedBreak`/`DuplicateOrdinaryBreak` 均断言 AIR、placement row、inventory、dropped registry、allocator、furniture/container/session、ward combat/VFX/SFX/audio 零变化。另覆盖 prepare 后 template/allocator/drop policy 故障、consumer registration 顺序扰动；成功时 receipt、PhysicalBlock AIR 或 EntityBacked Active→Removed、drop-or-inventory/furniture 同一 Update 安装且观察者不发第二掉落。另对 `DeadDropWardTriggered` 建穷尽 effect matrix：**owner active、non-owner active、inactive、missing owner、missing non-owner、duplicate terminal、prepare failure** 各列断言 block、entity、session、container return/contents drain/ground drop、combat/VFX/SFX/audio/receipt；missing owner/non-owner 分别等价 active owner/non-owner，缺失组件必须保持现有 fail-closed protection，禁止 ordinary fallback；inactive 仅真实 `ward_active=false`，不设 expired case。non-owner active/missing 必须保 block、Despawn entity、清 session、零 return/drain/drop，combat/AV/close 各一次且无 contents payload；duplicate terminal 零第二 side effect；prepare failure 零 mutation。
2. allocator/registry durability matrix 覆盖 ordinary→settlement、settlement→ordinary、两个并发 lease、lease 预留后 crash（未消费 ID 永久 gap）、restart sequence/lease high-water、`JS_SAFE_INTEGER_MAX` 边界与 exhausted fail-closed、同 composite/payload replay idempotent、强制同 ID 异 identity/hash collision（runtime registry、SQLite/outbox/hydration 全部零覆盖且 typed fail-closed）。测试不得以概率或扫描现存 JSON 证明唯一性；必须实际断言 sequence 线性化、lease 不重叠、全部 producer 同 API，以及 `DroppedLootRegistry::insert_checked`/SQLite insert-or-verify 的 absent/same/different 三分支与生产 direct insert/UPSERT static pin。
2. durable matrix 覆盖 online/offline、Prepared 后、terminal 后、live install 前后、canonical SQLite transaction 前后、ack 后、chunk 永不加载、未来加载、stable ground-drop entity 已/未 spawn；每格证明 reservation 有限释放、canonical after exactly once、projection once/延迟不重奖。再覆盖 source/manifest/station/skill/inventory hash/schema corruption、auto after/before repair、operator coherent CommitAfter/CompensateBefore、repair 中崩溃/重启、最终 normal writer 可写。
3. 真实 `main::build_server_app` block-authority matrix 覆盖 ordinary terrain IRON_ORE 与 manifest FanTie 分流、mineral non-exhausted/exhausted、reserved forge、manifest secondary-loss fault、stale ordinary snapshot、container/workbench terminal、spiritwood raw Start 与 social raw phase 表；并静态 pin raw reader 唯一。P1A 未 merge不得开 P1B；上述 ordinary/durable/repair/SkillSet/phase matrix 与 registration gate 全绿前不得开 P2。

## P2 — freshness、integrity、snapshot 唯一调度 ⬜

P2 独占 `apply_container_freshness_transition`、`sealed_vial=Halve`、`spirit_seal_box=Freeze`、`moisture_guard=SpoilOnly { rate: 0.3 }` 的 4×4 mapping；P1A/P1B 全部 writer 以 post-transition identity merge。持久 `integrity_lock(reason,detected_tick)`，唯一 `reconcile_container_integrity_freshness` 管 owner corruption。固定 `InventoryReconciliationSet::{Commit,SnapshotRequestProducer,Reconcile,Sweep,CollectSnapshots,EmitSnapshots}`：所有 P1A/P1B business/reconciliation writer 只 request；唯一 reader/outbox/emitter 每 entity/tick 最终一帧。P2 不 retry settlement、不补发 XP/物品、不把 settlement retry 混入 reconciliation。P2 固定在 `inventory::register`（`server/src/inventory/mod.rs:854`）以 `app.configure_sets(Update, (...).chain())` 注册这六个 set，并把 `reconcile_container_integrity_freshness`、唯一 request reader、outbox collector、emitter 分别 `in_set` 到对应阶段；在 `main::build_server_app`（`server/src/main.rs:71`）真实建 App 的 integration test 运行 Update，断言六 set 的 reader/collector/emitter 都已加载且顺序可观察，不能以静态数量计数替代运行接线。

- locked 期间该容器 behavior 强制 `Normal`；任何 active Freeze interval 整段 discard（`frozen_since_tick` 清空且**不**计入 `frozen_accumulated`），items 的位置/stack/layout 不变。
- 同 reason 重复检测零修改、revision 不变；reason 改变则更新 reason/detected_tick 并同样 discard active interval。
- repair 在**同一原子 commit** 清 `integrity_lock`：修复后 behavior=Freeze 则以新 `now_tick` 对每个 Freshness item `enter_container` 重开 interval，否则保持 None；首次 lock/reason-change/repair 各恰 +1，same-reason 0。
- 4×4 mapping 与上述强制 `Normal` 均锚回既有实现 `server/src/shelflife/container.rs`：`container_storage_multiplier`（:30，Halve 非 Stepwise 减半/Stepwise 退 Normal，Freeze time-based 0.0/Stepwise 必须 1.0**禁 0.0**）、`enter_container`（:82）、`exit_container`（:94，SpoilOnly{0.3} 仅 Spoil track/Decay·Age 退 Normal）。

**饱和测试**：逐格覆盖 4×4 freshness 转换及每个 P1A/P1B writer 的 raw→transition、merge identity、spill/drop；保存/加载三个 reason 各一条 integrity lock、首次/reason-change/repair revision +1 与 same-reason 0；静态 gate 断言唯一 `EventReader<InventorySnapshotRequest>` 和唯一 emitter；同 tick 多 P1A/P1B writer、多 reason、Added/Changed/join/revive、两个 entity 隔离、缺 client/serialize/send failure drain、最终 revision/content 一帧。任何 snapshot/reconcile 测试都不得重试 settlement 或重放经济副作用。

## P3 — 暗器迁移与恰好九个随身容器 ⬜

| # | canonical template | grid | capacity | slot | cost | accept | freshness |
|---|---|---|---:|---|---:|---|---|
|1|worn_grass_pouch|3×3|8.0|chest|0.008|empty/all|Normal|
|2|grass_pouch|3×3|10.0|chest|0.005|empty/all|Normal|
|3|ore_sack|3×3|10.0|chest|0.005|mineral|Normal|
|4|projectile_bag|3×4|10.0|legs|0.005|anqi|Normal|
|5|sealed_vial|2×2|8.0|chest|0.008|pill, food|Halve|
|6|spirit_seal_box|2×2|10.0|chest|0.005|treasure, pill|Freeze|
|7|moisture_guard|3×3|8.0|legs|0.008|empty/all|SpoilOnly { rate: 0.3 }|
|8|coin_box|3×3|8.0|chest|0.008|bonecoin|Normal|
|9|sealed_envelope|1×2|8.0|head|0.008|recipe_fragment, recipe_hint, scroll|Normal|

P3 的随身 `ContainerSpec` allowlist 必须与上表 **exact set equality**，恰好九个。`worn_grass_pouch` 与 `grass_pouch` 是已上线的 canonical 通用背包，分别保留既有 `default.toml` 起手 chest worn、`pack_grass_pouch → pack_<instance_id>` runtime container、`basic.grass_pouch` recipe 和旧存档 template/owner/container ID；**不** rename、映射或迁移它们，只补显式 `accept=[]`（或等价 all）和 P2 所需 `Normal` freshness metadata，保证可继续收纳多类别内容。`herb_pouch`/`herb_crate` 保持现有 misc craft outputs，P3 不将其升级为 `ContainerSpec`、不删除物品或配方、也不作 legacy 映射；`herb_crate_placed` 仍是独立 placeable twin，连同 `trade_crate`/`dead_drop_box` 均不进入九件。12 暗器迁 Anqi，`projectile_bag` 只收 Anqi；coin_box 只承诺骨币分类收纳便利。测试必须以**结构化 TOML parse 的真实 `[item.container]`**全 registry canonical ID exact-set equality 锁九件，不得被注释文本误导；六个 nonempty filter（ore/projectile/vial/seal/coin/envelope）分别有 accept/reject，三件 all（两通用背包+moisture_guard）分别有任意已知 category 的多类别正例及 malformed/unknown 负例；覆盖默认 loadout、`basic.grass_pouch` recipe、旧存档 save/load 的 canonical template/owner/container ID 不变，并断言不存在 `herb_pouch`/`herb_crate` ContainerSpec。

## P4 — filter wire、client 预提示、e2e 与归档 ⬜

`ContainerSnapshotV1.accept_filter` 固定为带 presence 的 `optional AcceptFilterV1 accept_filter`，精确同构生产 `ContainerAcceptFilter::{Category(ItemCategory),TemplatePrefix(String)}`：`AcceptFilterV1 { repeated AcceptFilterRuleV1 rules = 1; }`，其中 `AcceptFilterRuleV1` protobuf `oneof rule { ItemCategoryV1 category = 1; string template_prefix = 2; }`。Rust/TypeBox/Java 使用同名 strict tagged union；wrapper 缺失为协议错误，整份 snapshot fail-closed；wrapper present 且 `rules=[]` 表示 all；present 且 nonempty 按规则 OR，绝不降格为 categories-only 或把 prefix 当 category resolver fallback。**strict exactly-one 同时是 raw-wire 契约**：不得让 generated decoder 对 repeated oneof tag last-one-wins；只有先通过 `ProtoServerDataBridge` 对完整原始 `bong:server_data` payload 的受限 scanner、确认每条 rule 已知 member occurrence 恰一，才可 `parseFrom` 并桥接至 `InventorySnapshotHandler`。prefix 忠实 `server/src/inventory/mod.rs:2511-2534,3028-3041`：parser 对整项及 `prefix:` 后 payload `trim()`，拒绝空 payload，未 lower-case，`starts_with` 大小写敏感；wire present prefix 同样拒空或非法格式而不自行改写。`acceptance_lock` 保持 `optional`，healthy 时缺省，present 时为 `{reason: ContainerAcceptanceLockReason}`（canonical lower-snake `owner_missing`/`owner_not_found`/`owner_invalid`，与 `ItemCategoryV1`、`AcceptFilterRuleV1` 同级 Rust/protobuf/TypeBox/Java/client 同步）；locked 仍携 present `accept_filter`，不把 lock 当 filter presence。`InventoryItemViewV1.category` 仍由 P1A resolver 产 canonical lower-snake。JSON/TypeBox/Rust serde 与 Java parser 必须拒绝 null、extra key、多 tag、无 tag；protobuf binary 维持普通 unknown field 前向兼容，但 unknown enum、缺 oneof、非法 prefix 或缺 `accept_filter` wrapper 都 fail-closed。Rust `server/src/schema/inventory.rs::{ContainerSnapshotV1,InventoryItemViewV1,AcceptFilterV1,AcceptFilterRuleV1}`、`proto/bong/envelope.proto::{ContainerSnapshot,AcceptFilterV1,AcceptFilterRuleV1,ItemCategoryV1}`、`agent/packages/schema/src/inventory.ts::{ContainerSnapshotV1,AcceptFilterV1,AcceptFilterRuleV1,ItemCategoryV1}`、`client/.../InventorySnapshotHandler.java::parseContainers` 同名同步；wire scanner 的唯一 client 落点为 `client/src/main/java/com/bong/client/network/ProtoServerDataBridge.java::bridge`，在 `Envelope.ServerDataEnvelope.parseFrom(rawBytes)` 前执行。Worn/Pack/Inspect 仅以 `ContainerFilterRules.accepts` 做预提示，预测非法/locked 仍发 `InventoryMoveIntent`，client 不自推断，server 权威。P4 不接 snapshot routing。

**canonical `ItemCategory` exact set**：当前权威 `server/src/inventory/mod.rs:355-381` 的 `Pill`、`Herb`、`RecipeFragment`、`RecipeHint`、`Weapon`、`Armor`、`Treasure`、`BoneCoin`、`Tool`、`Scroll`、`Misc`、`Block`、`Mineral`、`Anqi`、`Liquid`、`Container`、`Food`、`Shield`，共 18 个；测试从该 exact set 穷举导出 18，禁止裸魔法数。synthetic Mineral 不是第 19 类：仅 P1A `resolve_item_category` 的 canonical mineral-id/registry/template 三重命中产生 `Mineral`，其 spoof/unknown/mismatch 为 typed invalid。bot/e2e 覆盖两通用背包仍收多类别、七专用容器过滤（含 ore_sack pass/herb reject、swap reverse、moisture_guard）、locked source/target、integrity revision、同 template 双 owner，并验证默认 loadout、`basic.grass_pouch` recipe、旧存档的 canonical template/owner/container ID 不变及无 `herb_pouch`/`herb_crate` ContainerSpec。

**饱和测试**：`AcceptFilterV1` raw protobuf wire pin 分别覆盖 wrapper 缺失（fail-closed）、wrapper present+empty（all）、category-only、prefix-only、category+prefix mixed OR、unknown enum、缺 oneof、非法/empty prefix（均 fail-closed）及普通 unknown binary field（protobuf 兼容忽略，不作为拒绝承诺）；**binary 的 strict oneof 规则不委托 generated decoder 的 last-one-wins**：在 `Envelope.ServerDataEnvelope.parseFrom` 前，`client/.../ProtoServerDataBridge.java::validateInventoryAcceptFilterRuleWire(byte[] raw)` 必须按 envelope field `8`（length-delimited `InventorySnapshot`）→ `InventorySnapshot.containers` field `2`（repeated length-delimited `ContainerSnapshot`）→ `ContainerSnapshot.accept_filter`（P4 新增的 length-delimited field）→ `AcceptFilterV1.rules` field `1`（repeated length-delimited `AcceptFilterRuleV1`）精确解包。scanner 只读取有限 varint tag/length：每层验证 field wire type=2、varint 最多 10 byte、length 非负且不越过当前 submessage 边界、嵌套/总 bytes 上限；截断、overflow、malformed length/varint 或错误 wire type 令整份 inventory snapshot fail-closed。每个 rule 只计已知 member：field `1` `category` 必 wire type 0，field `2` `template_prefix` 必 wire type 2；unknown field 以其 protobuf wire type 和 bounded length/varint 完整跳过且**不计数**。任何 rule 的 known-member occurrence 总数不是恰好 1（zero、category→prefix、prefix→category、category×2、prefix×2）均在 generated decode 前拒绝；single known+unknown 通过 scanner，随后仍由 enum/prefix 语义校验决定。server canonical encoder 必为每 rule 仅一 member tag；server Rust schema emitter、agent sample/raw-vector validator 与 client scanner 对同一 category→prefix、prefix→category、category×2、prefix×2、zero-known+unknown、single-known+unknown、malformed length/varint/wrong-wire-type vectors逐字节对拍，禁止只以 JSON 或已解码 object 覆盖 binary 语义。JSON/TypeBox/Rust serde/Java parser 分别覆盖 missing/null/extra-key/multi-tag/no-tag reject。穷举上文 exact `ItemCategory` set 的 18 项及 synthetic Mineral 正反；prefix trim、大小写敏感、非法格式的 production-authority pin；`ContainerAcceptanceLockReason` 三变体各自正反 round-trip、unknown reason 整份 snapshot fail-closed；Worn/Pack/Inspect 的 category/prefix/mixed/empty/footprint/lock `VALID`/`INVALID` 与仍发送 Intent；bot/e2e 对九容器逐一真实验证。

## §8 开放问题（历史表，全部已收口）

| # | 历史问题 | 状态 |
|---|---|---|
|1|平展 owner-instance 或 nested/session|§8.1 #1|
|2|resolver、mineral/forge source durability|§8.1 #2|
|3|P1 raw freshness 与 P2 transition|§8.1 #3|
|4|容器集合/矩阵|§8.1 #4|
|5|filter/lock client 预提示|§8.1 #5|
|6|剩余 PR 拆分|§8.1 #6|

> 原表仅供历史追溯；实施以 §8.1 为准。

## §8.1 决议（pre-P1 收口，2026-07-24）

### #1 owner-instance 平展模型唯一
**决议**：`PlayerInventory.containers` + `owner_instance_id`、strict resolver/migration 和 `InventoryMoveIntent` 为唯一架构，不恢复 nested/session。

**落点**：`server/src/inventory/mod.rs:469-482`（`ContainerState`）/ `server/src/inventory/mod.rs:6549-6592`（静态分类）+ 本 plan「基线」「P1A」。

### #2 resolver + durable source/WAL
**决议**：P1A 交付 process-local prepared core；ordinary break 固定为 `PreparedOrdinaryBreak` 的单 tick 单 commit owner：`BreakAttemptSnapshot` 必携 `TargetIdentity::{PhysicalBlock{dimension,pos,full BlockState},EntityBacked{placement_id,generation,dimension,pos,kind}}`，绝不以 entity bits/tick/session/AIR 取代 identity。P1B 新增持久 `entity_backed_placeable_placements`，ContainerBlock/WorkbenchBlock 创建/hydration/replacement 以 immutable placement ID + 单调 generation 作为唯一真相；commit 在所有实体/session/inventory/drop/allocator/ward AV 前全等复核，marker replacement、同址同 AIR、despawn/respawn、旧 terminal/replay、session 指向新 entity 一律 `StaleAuthorizedBreak` 零副作用。prepare 对 container policy 必产 `OrdinaryReturnAndDrain|DeadDropWardTriggered` typed disposition：生产 `container_block.rs:286-299` 的缺失 `DeadDropWard` 必按 `ContainerBlock.placed_by` 合成 active ward，missing owner/non-owner 分别等价 active owner/non-owner；非 owner（含 missing ward）保持 block、标记 entity `Despawned`、清 session、不返还/不 drain contents/不写 AIR，并由 ward receipt 唯一发 combat、VFX、SFX/audio；owner 与真实 inactive (`ward_active=false`) 走 ordinary return/drain，重复 terminal 零第二 effect。当前无 expiry 字段，本 plan 禁止 expired 分支；未来 expiry 必须另立 schema/migration plan。P1B 交付 `PreparedInventorySettlement`、UUIDv7-string `InventorySettlementId`、`DurableInstallReceipt`、`ProjectionOutbox`/`ProjectionReceipt`、SQLite `mineral_sources`（legacy exhausted JSON 一次导入后退役）、`durable_block_ownership`/`DurableBlockOwnershipIndex`（唯一物理 durable ownership）、mineral generation/sequence/recovery、`ForgeStationId`/SQLite `forge_stations`、`ForgeStationPlacementId` 原子扣 station item、escrow `ForgeAttemptId`。唯一状态机为 Prepared→CommittedPendingInstall→ack 后 CommittedStored/CommittedDroppedToGround/CommittedNoItem：terminal 不删 reservation；在线玩家先由 `SkillMutationGateway` 安装/receipt live candidate，随后同一 SQLite transaction 安装 canonical inventory/skill/source/station/manifest/WAL/outbox 并 ack；离线直接 canonical install+ack。ground outbox canonical key 固定 `(settlement_id TEXT UUIDv7, payload_ordinal INTEGER)` + SQLite `UNIQUE(settlement_id,payload_ordinal)`；`InventoryInstanceIdAllocator` durable SQLite CAS sequence + range lease 是普通 runtime、migration、craft/drop/give、settlement canonical transaction 与 `GroundProjectionSurrogateId` 的**唯一** allocation authority：所有 lease 在 `BEGIN IMMEDIATE` 从唯一 high-water 原子预留不重叠 `[start,end]` 并记录 lease_id/owner/process_epoch/start/end/next/consumed，崩溃未用 ID 永久跳过；settlement 与 outbox row 在同 transaction 从同一 sequence reserve/consume，禁止第二 allocator/high-water 或直接自增；MAX exhausted fail-closed。所有 registry/SQLite insert 均 insert-or-verify（absent insert、same complete identity/hash idempotent、different typed fail-closed），禁止 HashMap/UPSERT 覆盖；全生产 producer/insert 清单与 static pin 见 P1B B.6-9。chunk/ECS/entity/future hydration 只用该 surrogate 投影、并验证复合键+payload hash，永不阻塞 reservation 释放；WAL/receipt 保存 immutable before/after、schema/hash/revision/install marker；`QuarantinedCorrupt→RepairPrepared→CommittedPendingInstall|CompensatedTerminal` 只由 server-side `SettlementRepairAuthority`+`SettlementRepairRecord` 修复，自动 after/before 规则固定，mixed 必须 operator coherent snapshot。固定仅 `authorize_block_break_intents(DiggingEvent)` 读取 raw dig，发携 immutable original `BlockState`/dimension/pos/direction/game mode/tool context 与 `raw_packet_phase`/`synthetic_completion`/dispatch receipt 的 `AuthorizedDiggingPhase`；Start/Abort/Terminal 以 epoch/sequence `BreakAttemptId` 关联，重复 Start supersede、无 Start terminal orphan、Creative Start 派生 synthetic Terminal。manifest miss 一律 Ordinary，即使 state 为 `IRON_ORE`/ANVIL，hit 后才校验 source/index/ECS/block projection。mineral 保留 Start/Abort/Stop/Creative 生产语义，spiritwood 仅 Survival Start、container/workbench 仅 Terminal；workbench 打开仍独立 `WorkbenchOpenRequest` 三格校验；social 维持 raw Start 与 raw Stop 各一次 reveal/intrusion，Creative synthetic Terminal 不二投递。`SkillMutationGateway` 迁移 `consume_skill_xp_gain`、scroll consume、`reset_for_new_character` 和 dev reset，所有 settlement 窗口 typed `SettlementReserved`。manifest 尚存时任一 secondary projection loss 是 typed integrity fault；manifest 与全部 secondary metadata 同时丢失时 raw state 无法区分 ordinary，交 migration/integrity sweep/backup/manual quarantine，禁止 DiggingEvent 猜测。P4 的 `AcceptFilterV1` 固定为 `rules: repeated AcceptFilterRuleV1` tagged category/template_prefix union，忠实生产 trim、empty reject、case-sensitive prefix 与 OR，而非 categories-only；client 只提示不授权。现有 mineral direct consumer/forge direct grant 只为待替基线；禁止 consumer-first pending、source-event 重发、runtime session key、generic durable commit 和端到端一次通知承诺。

**落点**：`server/src/mineral/anchors.rs`、`persistence.rs`、`respawn.rs`、`break_handler.rs:169-275`（迁为 `AuthorizedDiggingPhase` 的 Start/Abort/Terminal consumer）/ `server/src/mineral/inventory_grant.rs:38-138`（direct consumer）/ `server/src/forge/station.rs:26-165`（现有 ECS placement projection）/ `server/src/forge/mod.rs:182-384`、`server/src/forge/session.rs:81-222`（runtime attempt baseline）/ `server/src/world/block_break.rs:33-58`（替为 `authorize_block_break_intents`、attempt snapshot 与 phase envelope）/ `server/src/world/block_drop.rs:160-277`（替为 Terminal snapshot consumer，移除 index-miss/live-layer authority）/ `server/src/world/container_block.rs:186`、`server/src/craft/workbench.rs:165`、`server/src/spiritwood/mod.rs:83-166`、`server/src/social/mod.rs:2130-2175`（各自 phase matrix consumer）/ `server/src/persistence/mod.rs:1083-2351`（`durable_block_ownership`、`mineral_sources`/`forge_stations`、settlement receipt migration）/ `server/src/player/state.rs:1313-1375,2342-2367`（load/transaction helper）+ 本 plan「P1A」「P1B」。

### #3 P1 raw freshness，P2 transition
**决议**：P1A/P1B hook 原样携 freshness；P2 独占 transition、integrity、reconciliation 和 six-stage snapshot。P2 观察 committed writers，不 retry settlement/补发经济。

**落点**：`server/src/shelflife/container.rs:29-98` / `server/src/spiritwood/mod.rs:611-647` / `server/src/inventory/mod.rs` + 本 plan「P1A」「P1B」「P2」。

### #4 九容器固定
**决议**：P3 仅上表恰好九个 canonical ContainerSpec、12 暗器和骨币分类文案：既有通用 `worn_grass_pouch`/`grass_pouch` 原样占两席，仅补显式 all filter 与 Normal metadata，保持 default loadout/recipe/runtime pack/旧存档 ID；其余七席为新增专用件。`herb_pouch`/`herb_crate` 保持 misc craft outputs，不升级、删除或映射为 ContainerSpec；placeable 边界不变。

**落点**：`server/assets/items/core.toml:383-419`（两种既有通用背包与显式 all/Normal）/ `server/assets/inventory/loadouts/default.toml`（起手 worn）/ `server/src/craft/mod.rs:878-885`（`basic.grass_pouch`）/ `server/src/player/state.rs:1313-1420`（既有 canonical template/owner/container load invariants）/ `server/assets/items/workbench_materials.toml`（保持 misc 的 `herb_pouch`/`herb_crate`）/ TOML item registry structured parse test + 本 plan「P3」「范围边界」。

### #5 P4 仅 wire/client
**决议**：P4 投影 P2 lock、filter/category，并在三 surface 预提示；不新增 snapshot routing。`AcceptFilterRuleV1` 的 strict exactly-one 延伸到 binary：server canonical encoder 每 rule 只发一个 known member tag；client `ProtoServerDataBridge.bridge(rawBytes)` 必在 generated envelope decode 前按 envelope `8`→inventory `2`→container `accept_filter`→filter `rules=1` 的 length-delimited 边界扫描 member `category=1`/varint、`template_prefix=2`/length-delimited；每 rule known occurrence 必恰一，zero、mixed、重复、malformed varint/length/wrong wire type 整 snapshot fail-closed，ordinary unknown field bounded-skip 且 forward-compatible。Rust/server emitter、agent samples 和 client scanner 对同一 raw vector 对拍，JSON strict multi-tag 规则不变。

**落点**：`server/src/schema/inventory.rs:241-287` / `proto/bong/envelope.proto:688-695` / `agent/packages/schema/src/inventory.ts:322-363` / `client/src/main/java/com/bong/client/network/InventorySnapshotHandler.java:139-168` + 本 plan「P4」。

### #6 P1A→P1B→P2→P3→P4 五 PR 严格串行
**决议**：剩余恰好五 PR：PR-1/P1A、PR-2/P1B、PR-3/P2、PR-4/P3、PR-5/P4+e2e+真实 Finish Evidence+归档。严格前序 merge 后才开下一 PR；P1B crash matrix 是 P2 前置；P4 仅在 PR-5 全部满足后归档，不能提前完成。

**落点**：本 plan §10。

## §10 实施工作流

### §10.1 串行不变量
1. 每 PR 前 `git fetch origin`，核验上一 PR merge，以最新 `origin/main` 建独立 branch/worktree。
2. 一 PR 只做当前阶段，不夹带下一阶段或其它 plan；每次 HEAD 变化重跑受影响 validator/gate/review。
3. P1A merge 前不开 P1B；P1B WAL/crash/recovery/exactly-once economics matrix 通过前不开 P2；P4 e2e 后才写真实 Evidence 和归档，并在归档新 HEAD 完整复验。

### §10.2 五个严格串行 PR
1. **PR-1 / P1A**：owner migration/resolver/category、process-local `PreparedInventoryMutation`、owned settlement preparation、ordinary/non-durable staged writers 与 identity freshness；不动 snapshot sender，不做 durable source commit。
2. **PR-2 / P1B**：`PreparedOrdinaryBreak` 单一 ordinary terminal commit、settlement ID、SQLite `mineral_sources`/legacy JSON 一次导入退役、generation/extraction/recovery/AIR gate、`ForgeStationId`/`forge_stations`、`ForgeStationPlacementId` 原子扣物、escrow `ForgeAttemptId`、before/after WAL、`DurableInstallReceipt` canonical transaction、`ProjectionOutbox` 延迟投影、`SettlementRepairAuthority`/`SettlementRepairRecord`、`SkillMutationGateway` 的全 writer migration、station placement/break/hydrator、ECS install/hydration/retry、raw reader/phase matrix 以及完整 crash/repair/gate matrix；不接 P2 snapshot routing，不扩展其它 placeable。
3. **PR-3 / P2**：freshness transition、integrity/reconciliation、六阶段 snapshot pipeline，覆盖 P1A/P1B writers，不重算 settlement。
4. **PR-4 / P3**：九容器 TOML 的 structured-parse registry exact-set gate、既有 `worn_grass_pouch`/`grass_pouch` 的 explicit-all/Normal metadata 与 canonical ID 不变回归、七新增专用件、保持 misc 的 `herb_pouch`/`herb_crate`、12 Anqi、coin 文案与 filter tests；不含 placeable 转换。
5. **PR-5 / P4**：filter/lock/category wire、Worn/Pack/Inspect、bot/九容器 e2e；全部验收后填写真实 Finish Evidence、阶段状态与归档。归档 commit 后重跑 exact-HEAD validator、门禁、CI、`/review`、CodeRabbit。

### §10.3 完成条件
仅当五 PR 严格按序 merge，P1B crash matrix、P2 reconciliation/snapshot、P3 九容器、P4 cross-stack e2e 全实证通过，且 PR-5 填满以下 Evidence 并在归档新 HEAD 复验后，本 plan 才完成。

## Finish Evidence

> 待未来 PR-5 实际消费填写；不得预填测试、日期、commit 或完成声明。

### 落地清单
- 待填写：各阶段真实文件与 symbols。

### 关键 commit
- 待填写：hash、日期、阶段与理由。

### 测试结果
- 待填写：实际命令、数量、P1B crash matrix 与 P4 e2e。

### 跨仓库核验
- 待填写：server / schema / protobuf / client 实际命中 symbols。

### 遗留 / 后续
- 待填写：本 plan 外明确依赖与未覆盖项。
