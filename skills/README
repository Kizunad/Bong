# skills

本目录是全仓库 agent skill（slash command）的**唯一 canonical 目录**，经 `.claude/skills → ../skills` symlink 暴露给 Claude Code。历史上曾由 `.pi/` `.opencode/` `commands/` 多方共享，现已全部收敛到这里。

## 结构

```
skills/                       ← 唯一 canonical（直接改这里）
├── audit-plans-progress/     ← 审计计划进度
├── browser-cdp/              ← 浏览器 CDP 操控（来自 oh-story-claudecode）
├── bugfix/                   ← 持续调度 bug skeleton 闭环
├── bughunt/                  ← 自治 bug 狩猎一轮
├── consume-plan/             ← plan 消费流水线（worktree → 实施 → PR → merge）
├── deduce-tale/              ← 第一人称剧情推演
├── gen-image/                ← 生成图片
├── library-lore/             ← 图书馆 lore 查阅
├── plans-status/             ← 计划状态总览
├── pr-watch/                 ← PR 监控
├── review-book/              ← 书籍审查
├── runwebui/                 ← 模块图谱 webui
├── story-deslop/             ← 网文去AI味润色（来自 oh-story-claudecode）
├── story-long-analyze/       ← 长篇网文拆文（来自 oh-story-claudecode）
├── story-long-scan/          ← 长篇网文扫榜（来自 oh-story-claudecode）
├── story-long-write/         ← 长篇网文写作（来自 oh-story-claudecode）
├── story-short-analyze/      ← 短篇网文拆文（来自 oh-story-claudecode）
├── story-short-scan/         ← 短篇网文扫榜（来自 oh-story-claudecode）
├── story-short-write/        ← 短篇网文写作（来自 oh-story-claudecode）
├── write-book/               ← 书籍撰写
├── write-tale/               ← 末法残土叙事写作（修士手记协议）
└── ...                       ← 后续新增 skill 放这里
```

## symlink 映射

```
.claude/skills/ → ../skills/
```

## 约定

- 所有 skill 的 **唯一真实副本** 在 `skills/`
- `.claude/skills/` 为目录级 symlink，新增/删除子目录自动跟随
- 新增 skill：直接在 `skills/` 下创建 `skill-name/SKILL.md`
- 删除 skill：直接 `rm -rf skills/<name>/`
- **禁止** 通过 symlink 路径编辑文件——统一在 `skills/` 下操作
