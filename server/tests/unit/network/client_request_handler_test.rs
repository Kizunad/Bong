//! CRH tests requiring the private Bevy system parameter are registered from
//! `src/network/client_request_handler_tests.rs`; this target remains registered
//! so the Cargo test layout is explicit and future public-contract tests have a
//! stable destination.
#![allow(dead_code)]
