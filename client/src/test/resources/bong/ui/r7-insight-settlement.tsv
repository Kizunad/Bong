# terminal_cause	settlement	owner	identity_rule	observable_effect
ACCEPT	accept(selectedOptionId)	InsightOfferScreen	settle only when current offer triggerId matches	Emit exactly one accepted InsightDecision and close this screen if still current.
DECLINE	decline	InsightOfferScreen	settle only when current offer triggerId matches	Emit exactly one declined InsightDecision and close this screen if still current.
TIMEOUT	timedOut	InsightOfferScreen	settle only when nowMs is greater than or equal to expiresAtMs for the same triggerId	Emit exactly one timed-out InsightDecision and close this screen if still current.
ESC	decline	InsightOfferScreen	settle only when current offer triggerId matches	Emit exactly one declined InsightDecision; subsequent removed callback is ignored.
REPLACED_BY_DIFFERENT_OFFER	decline	InsightOfferScreen + InsightOfferScreenBootstrap + ScreenTransitionController.CurrentScreenCancellationHandler	bootstrap compares outgoing and replacement triggerId before transition; handler settles the outgoing identity once	Emit exactly one declined InsightDecision before the new offer becomes authoritative.
REMOVED_EXCEPTIONALLY	decline	InsightOfferScreen.removed	settle the same triggerId when no prior terminal cause won	Emit exactly one declined InsightDecision even if removal bypasses close.
DUPLICATE_TERMINAL	NOOP	InsightOfferScreen	settled triggerId and first terminal cause are immutable	Emit no second decision and do not mutate the winning cause.
